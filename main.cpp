// Командный проект. Группа ПИ-53.
// Команда: Левина (в. 43, техлид), Прощалыгина (в. 68), Старченко (в. 88).
#include <iostream>
// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
#include "proschalygina.h"
#include "levina.h"
// #include "starchenko.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===
using namespace std;

int main() {
    int choice, l, gal, n;
    do {
        cout << "\n=== Командный проект: сборник расчётов ===\n";
        // === БЛОК МЕНЮ: каждый участник добавляет свои пункты ===
        cout << "1. Факториал\n";
        cout << "2. Двойной факториал\n";
        
        cout << "3. Из литров в галлоны\n";
        cout << "4. Из галлонов в литры\n";
        // === КОНЕЦ БЛОКА МЕНЮ ===
        cout << "0. Выход\n";
        cout << "Выберите пункт: ";
        cin >> choice;

        switch (choice) {
            // === БЛОК ОБРАБОТКИ: каждый участник добавляет свои case ===
            case 1:
                cout << "Введите число: ";
                cin >> n;
                cout << "Факториал " << n << " равен " << factorial(n) << "\n";
                break;
		    case 2:
                cout << "Введите число: ";
                cin >> n;
                (n  <= 20) ? cout << "Двойной факториал " << n << " равен " << doubleFactorial(n) << "\n" : cout << "Число должно быть меньше 20\n";	
                break;
            
        case 3:
            cout << "Введите количество литров: ";
            cin >> l;
            cout << " " << lToGal(l) << " галлонов\n";
            break;
        case 4:
            cout << "Введите количество галлонов: ";
            cin >> gal;
            cout << " " << galToL(gal) << " литров\n";
            break;
            // === КОНЕЦ БЛОКА ОБРАБОТКИ ===
            case 0:
            cout << "Работа завершена.\n";
            break;
            default:
            cout << "Такого пункта нет.\n";
        }
    } while (choice != 0);

    return 0;
}
